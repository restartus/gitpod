packages:
  - name: app
    type: yarn
    srcs:
      - "src/**"
      - "public/**"
      - package.json
      - webpack.config.js
      - tsconfig.json
    config:
      dontTest: true
      yarnLock: ${coreYarnLockBase}/../yarn.lock
